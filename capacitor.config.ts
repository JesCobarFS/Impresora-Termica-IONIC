import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prueba13.app',
  appName: 'prueba13',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
